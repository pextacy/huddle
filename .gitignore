# Dependencies
node_modules/

# Next.js frontend build output
web/.next/
web/out/
web/next-env.d.ts

# Pear / Bare local-first storage (never commit ledger/wallet state)
.pear/
store/
storage/

# Secrets — seed phrases live only in device secure storage, never in the repo
*.seed
.env
.env.*

# Generated wallet preview (rendered from live on-chain data)
wallet-preview.html

# OS / editor
.DS_Store
*.log
